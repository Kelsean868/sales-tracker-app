rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // --- Helper Function ---
    function isSignedIn() {
      return request.auth != null;
    }
    
    // --- Rules for all collections ---
    // This allows any user that is signed in to read the data.
    match /users/{userId} {
      allow read: if isSignedIn();
      allow update: if request.auth.uid == userId;
    }
    match /activities/{activityId} {
      allow read, write: if isSignedIn();
    }
    match /leads/{leadId} {
      allow read: if isSignedIn();
      allow create, update, delete: if isSignedIn() && isOwner(resource.data);
    }
    match /clients/{clientId} {
      allow read, write: if isSignedIn();
    }
    match /contacts/{contactId} {
      allow read, write: if isSignedIn();
    }
    match /policies/{policyId} {
      allow read, write: if isSignedIn();
    }
    match /goals/{goalId} {
      allow read, write: if isSignedIn();
    }
    match /leaderboard/{entry} {
        allow read, write: if isSignedIn();
    }
     match /regions/{docId} { allow read: if isSignedIn(); allow write: if false; }
    match /branches/{docId} { allow read: if isSignedIn(); allow write: if false; }
    match /units/{docId} { allow read: if isSignedIn(); allow write: if false; }
    match /teams/{docId} { allow read: if isSignedIn(); allow write: if false; }
    match /workHistory/{docId} {
      allow read, create: if isSignedIn() && request.auth.uid == resource.data.userId;
    }
  }
}
